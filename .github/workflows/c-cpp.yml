name: C/C++ CI

on:
  push:
    branches: [ master precheck ]
  pull_request:
    branches: [ master precheck ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: configure
      run: ./configure
    - name: make
      run: make
    - name: install libjson-c
      run: sudo apt-get install libjson-c-dev libjson-c4
    - name: make check
      run: make test

